{
  "name": "bilibili-ai-filter",
  "version": "1.0.0",
  "description": "B站视频AI筛选器 - 读取视频标题交给AI分析决定是否重定向",
  "scripts": {
    "build": "node scripts/inject-env.mjs && tsc",
    "watch": "node scripts/inject-env.mjs && tsc --watch"
  },
  "devDependencies": {
    "@types/firefox-webext-browser": "^143.0.0",
    "typescript": "^5.3.0"
  }
}
